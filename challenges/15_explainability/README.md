# SHAP Happens

A deterministic, local-only hard challenge in Explainable AI. Use the platform challenge console or POST /api/challenges/<id>/run. All records and effects are synthetic; no network, shell, or external files are used.

Objective: Change the top explanation without changing the prediction.
