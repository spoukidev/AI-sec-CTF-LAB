# Say It Differently

A deterministic, local-only easy challenge in LLM Security. Use the platform challenge console or POST /api/challenges/<id>/run. All records and effects are synthetic; no network, shell, or external files are used.

Objective: Express the request without blocked keywords.
