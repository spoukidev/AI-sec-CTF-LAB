# Trusting the Robot

A deterministic, local-only medium challenge in AI Application Security. Use the platform challenge console or POST /api/challenges/<id>/run. All records and effects are synthetic; no network, shell, or external files are used.

Objective: Manipulate generated JSON into an internal-alert action.
