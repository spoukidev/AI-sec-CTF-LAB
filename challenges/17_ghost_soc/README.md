# Ghost in the SOC

A deterministic, local-only expert challenge in AI SOC / LLM Security. Use the platform challenge console or POST /api/challenges/<id>/run. All records and effects are synthetic; no network, shell, or external files are used.

Objective: Chain retrieved injection into analysis and a simulated privileged action.
