# Feature Shift

A deterministic, local-only hard challenge in Adversarial Machine Learning. Use the platform challenge console or POST /api/challenges/<id>/run. All records and effects are synthetic; no network, shell, or external files are used.

Objective: Move allowed numeric features across a synthetic classifier boundary.
