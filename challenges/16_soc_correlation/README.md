# The Wrong Incident

A deterministic, local-only hard challenge in AI SOC Security. Use the platform challenge console or POST /api/challenges/<id>/run. All records and effects are synthetic; no network, shell, or external files are used.

Objective: Make unrelated alerts share one incident.
