# Confidence Is Not Severity

A deterministic, local-only medium challenge in AI SOC Security. Use the platform challenge console or POST /api/challenges/<id>/run. All records and effects are synthetic; no network, shell, or external files are used.

Objective: Separate model probability from business severity.
