import type {Challenge,Profile} from './types'
const json=async<T>(path:string,options?:RequestInit):Promise<T>=>{const r=await fetch(path,options);if(!r.ok)throw new Error(await r.text());return r.json()}
export const api={challenges:()=>json<Challenge[]>('/api/challenges'),challenge:(id:string)=>json<Challenge>(`/api/challenges/${id}`),profile:(player:string)=>json<Profile>(`/api/profile/${encodeURIComponent(player)}`),scoreboard:()=>json<Array<{player:string;solved:number;points:number}>>('/api/scoreboard'),run:(id:string,payload:Record<string,unknown>)=>json<Record<string,unknown>>(`/api/challenges/${id}/run`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({payload})}),submit:(id:string,player:string,flag:string)=>json<{correct:boolean;message:string}>(`/api/challenges/${id}/submit`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({player,flag})})}
